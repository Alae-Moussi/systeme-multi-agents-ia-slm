```yaml
version: '3'
services:
  frontend:
    image: nginx:light
    ports:
      - "80:80"
    volumes:
      - ./frontend:/var/www/html
  backend:
    build: .
    ports:
      - "5000:5000"
    depends_on:
      - frontend
    volumes:
      - ./backend:/app
```